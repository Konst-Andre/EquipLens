> живе доки: назавжди (роль теки)

# sessions/ — живі самері EquipLens

Рівно 2 живі самері (`EquipLens_session_summary_S<N>_*.md`, wsd 1.8); §0 «ВІДКРИТЕ» — зверху, §6 — стартове повідомлення.
Витіснене самері — `git mv` в `archive/summaries/`, не видаляється. Код старший за самері (wsd 12.1).
