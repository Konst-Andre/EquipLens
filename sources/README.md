> живе доки: назавжди (роль теки)

# sources/ — вихідні дані

Оригінали, з яких збираються `docs/data/` і `docs/media/`: xlsx, zip, фото. Не публікуються.
Якщо дані приватні — репо стає приватним цілком; у `docs/` потрапляє тільки зібране.
