> живе доки: назавжди (роль теки)

# tools/ — стенди, смоуки, скрипти

Головний стенд `EquipLens_headbench_v*.html` і матриця `smoke_v*.js` — живий = найбільший номер під маскою (gov 12.20).
Смоук ганяти в обох режимах: `node smoke_vN.js` · `node smoke_vN.js --inject`. HTML перед видачею — `Lens_validate.py --html`.
Витіснений стенд — `git mv` в `archive/stands/`.
